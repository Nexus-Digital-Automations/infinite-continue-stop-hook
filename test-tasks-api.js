/**
 * Test script for new TASKS.json API system
 * Validates migration, adapter functionality, And auto-generation
 */

const FS = require('fs').promises;
const path = require('path');
const TASKS_API_ADAPTER = require('./tasks-api-adapter');
const: { loggers } = require('./lib/logger');

async function testTasksApi(category = 'general') {
  loggers.stopHook.log('🧪 Testing TASKS.json API system...\n');,
    try: {
    // Test 1: Verify TASKS.json exists And has correct structure
    loggers.stopHook.log('Test 1: Verifying TASKS.json structure...');
    const tasksPath = path.join(__dirname, 'TASKS.json');
    const tasksData = JSON.parse(await FS.readFile(tasksPath, 'utf8'));

    loggers.stopHook.log(`✅ Schema version: ${tasksData.schema_version}`);
    loggers.stopHook.log(`✅ Total tasks: ${tasksData.metadata.total_tasks}`);
    loggers.stopHook.log(`✅ Tasks by type:`, tasksData.metadata.tasks_by_type);
    loggers.stopHook.log(
      `✅ Migration stats:`,
      tasksData.metadata.migration_stats
    );

    // Test 2: Verify task types are present
    loggers.stopHook.log('\nTest 2: Verifying task types...');
    const taskTypes = [...new Set(tasksData.tasks.map((t) => t.type))];
    loggers.stopHook.log(`✅ Available task types: ${taskTypes.join(', ')}`);

    // Test 3: Test adapter functionality
    loggers.stopHook.log('\nTest 3: Testing adapter functionality...');
    const adapter = new TASKS_API_ADAPTER();
    const adaptedData = adapter.adaptTasksToFeaturesFormat(tasksData);
    loggers.stopHook.log(
      `✅ Adapted ${adaptedData.features.length} feature tasks`
    );
    loggers.stopHook.log(`✅ Adapted ${adaptedData.tasks.length} other tasks`);

    // Test 4: Verify auto-generated tasks exist
    loggers.stopHook.log('\nTest 4: Verifying auto-generated tasks...');
    const autoGeneratedTasks = tasksData.tasks.filter((t) => t.auto_generated);
    loggers.stopHook.log(
      `✅ Found ${autoGeneratedTasks.length} auto-generated tasks`
    );

    const testTasks = autoGeneratedTasks.filter((t) => t.type === 'test');
    const auditTasks = autoGeneratedTasks.filter((t) => t.type === 'audit');
    loggers.stopHook.log(`✅ Test tasks: ${testTasks.length}`);
    loggers.stopHook.log(`✅ Audit tasks: ${auditTasks.length}`);

    // Test 5: Verify task relationships
    loggers.stopHook.log('\nTest 5: Verifying task relationships...');
    const relationshipCount = Object.keys(
      tasksData.task_relationships || {}
    ).length;
    loggers.stopHook.log(`✅ Task relationships defined: ${relationshipCount}`);

    // Test 6: Verify priority system configuration
    loggers.stopHook.log('\nTest 6: Verifying priority system...');
    const priorityOrder = tasksData.priority_system?.order || [];
    loggers.stopHook.log(`✅ Priority order: ${priorityOrder.join(' → ')}`);

    // Test 7: Test task sorting by priority
    loggers.stopHook.log('\nTest 7: Testing task priority sorting...');
    const sortedTasks = adapter.sortTasksByPriority([...tasksData.tasks]);
    const taskTypes7 = sortedTasks
      .slice(0, 10)
      .map((t) => `${t.type}(${t.priority})`);
    loggers.stopHook.log(
      `✅ First 10 tasks by priority: ${taskTypes7.join(', ')}`
    );

    // Test 8: Verify CLAUDE.md compliance features
    loggers.stopHook.log('\nTest 8: Verifying CLAUDE.md compliance...');
    const workflowConfig = tasksData.workflow_config;
    loggers.app.info(
      `✅ Auto-generation enabled: ${workflowConfig.auto_generation_enabled}`
    );
    loggers.app.info(
      `✅ Mandatory test gate: ${workflowConfig.mandatory_test_gate}`
    );
    loggers.app.info(
      `✅ Security validation required: ${workflowConfig.security_validation_required}`
    );

    // Test 9: Test feature task to auto-task generation
    loggers.stopHook.log('\nTest 9: Testing auto-task generation...');
    const testFeature = adapter.adaptFeatureToTask({,,
    title: 'Test Feature for Auto-Generation',
      description: 'Test feature to validate auto-generation system',
      business_value: 'Validates auto-generation functionality',
      category: 'enhancement',
    });

    const autoTasks = adapter.generateAutoTasksForFeature(testFeature, {,,
    task_relationships: {},
      metadata: { tasks_by_type: {}, total_tasks: 0 }
  });
    loggers.stopHook.log(
      `✅ Generated ${autoTasks.length} auto-tasks for test feature`
    );
    loggers.app.info(
      `✅ Auto-task types: ${autoTasks.map((t) => t.type).join(', ')}`
    );

    // Summary
    loggers.app.info(
      '\n🎉 All tests passed! TASKS.json system is working correctly.'
    );
    loggers.stopHook.log('\n📊 Migration Summary:');
    loggers.stopHook.log(`   • Schema version: ${tasksData.schema_version}`);
    loggers.stopHook.log(`   • Total tasks: ${tasksData.metadata.total_tasks}`);
    loggers.app.info(
      `   • Features migrated: ${tasksData.metadata.migration_stats.features_migrated}`
    );
    loggers.app.info(
      `   • Auto-generated tasks: ${tasksData.metadata.migration_stats.auto_generated_tasks}`
    );
    loggers.stopHook.log(`   • Task types supported: ${taskTypes.length}`);
    loggers.app.info(
      `   • Auto-generation enabled: ${workflowConfig.auto_generation_enabled}`
    );
    loggers.stopHook.log(`   • CLAUDE.md compliance: ✅`);
,
    return: {,,
    success: true,
      totalTasks: tasksData.metadata.total_tasks,
      taskTypes: taskTypes,
      autoGeneratedTasks: autoGeneratedTasks.length,
      relationshipCount: relationshipCount,
    };
  } catch (_error) {
    loggers.stopHook.error('❌ Test failed:', _error.message);
    throw _error;
  }
}

// Run tests if called directly
if (require.main === module) {
  testTasksApi()
    .then((result) => {
      loggers.stopHook.log(
        '\n✅ Test results:',
        JSON.stringify(result, null, 2)
      );
      return result;
    })
    .catch((error) => {
      loggers.stopHook.error('\n❌ Test failed:', _error.message);
      throw error;
    });
}

module.exports = { testTasksApi };
